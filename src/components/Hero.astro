---
interface Props {
  lang: "pl" | "en";
}

const { lang } = Astro.props;

const content =
  lang === "pl"
    ? {
        title: 'Wsparcie, nie etykiety. Rozwiązania, nie "diagnoza na siłę".',
        subtitle:
          'Szukasz praktycznego wsparcia w Bolesławcu lub online i chcesz podejścia bez "szpitalnej atmosfery"? Oferujemy counselling i trening psychospołeczny — pracę nad stresem, relacjami, emocjami i codziennym funkcjonowaniem. Bez rozpoznań medycznych i bez leczenia; gdy potrzebujesz konsultacji lekarskiej, pomożemy dobrać właściwą ścieżkę.',
        ctaPrimary: "Umów Sesję",
        ctaServices: "Zobacz usługi",
        ctaPrices: "Zobacz ceny",
        servicesLink: "/uslugi",
        pricesLink: "/cennik",
      }
    : {
        title: 'Support, not labels. Solutions, not "forced diagnosis".',
        subtitle:
          'Looking for practical support in Bolesławiec or online—without the "hospital vibe"? We offer counselling and psychosocial training to work on stress, relationships, emotions, and everyday functioning. No medical diagnoses and no treatment; if medical support is needed, we help you choose the right pathway.',
        ctaPrimary: "Book a Session",
        ctaServices: "View Services",
        ctaPrices: "View Prices",
        servicesLink: "/en/services",
        pricesLink: "/en/pricing",
      };
---

<section class="hero">
  <div class="container hero-content animate-fade-in">
    <h1>{content.title}</h1>
    <p class="subtitle">{content.subtitle}</p>
    <div class="cta-group">
      <a
        href="https://rejestracja.psychopedagog.org/#/booking"
        target="_blank"
        class="btn btn-accent hero-btn">{content.ctaPrimary}</a
      >
      <a href={content.servicesLink} class="btn btn-secondary hero-btn"
        >{content.ctaServices}</a
      >
      <a href={content.pricesLink} class="btn btn-outline hero-btn"
        >{content.ctaPrices}</a
      >
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 6rem 0;
    overflow: hidden;
    /* Background handled globally by BackgroundAnimation */
    min-height: 80vh;
    display: flex;
    align-items: center;
  }

  .hero-content {
    position: relative;
    z-index: 10;
    max-width: 800px;
  }

  h1 {
    font-size: clamp(2.5rem, 5vw, 4.5rem);
    margin-bottom: 1.5rem;
    color: var(--color-primary);
    line-height: 1.1;
  }

  .subtitle {
    font-size: 1.25rem;
    margin-bottom: 2.5rem;
    color: var(--color-text-light);
    max-width: 600px;
  }

  .cta-group {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-btn {
    padding: 1rem 2.5rem;
    font-size: 1.1rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 15px rgba(220, 88, 42, 0.3);
  }

  .btn-secondary {
    background-color: var(--color-secondary);
    color: var(--color-text);
  }

  .btn-outline {
    background-color: transparent;
    border: 2px solid var(--color-primary);
    color: var(--color-primary);
  }

  .hero-btn:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 10px 25px rgba(220, 88, 42, 0.5);
    filter: brightness(1.1);
    color: black;
  }

  /* Blob styles removed - using global BackgroundAnimation */
</style>
